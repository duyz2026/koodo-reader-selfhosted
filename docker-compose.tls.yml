# HTTPS 模式（TLS 模式 1/2）使用
# 端口职责：HTTP/HTTPS -> 网页版；DS_PORT -> 数据源（与网页版分离，安卓端不支持路径前缀）
services:
  caddy:
    ports:
      - "${HTTP_PORT:-80}:80"
      - "${HTTPS_PORT:-443}:443"
      - "${DS_PORT:-8091}:${DS_PORT:-8091}"
