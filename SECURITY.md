# 安全策略

## 报告漏洞

请**不要**通过公开 issue 报告安全漏洞。改为：

- 邮件：security@koodo-sync-hub.example（占位，维护者替换）
- 或 GitHub 私信维护者

我们会在 7 天内响应，并协调修复与负责任披露。

## 默认安全基线

本工具安装即遵循以下基线，使用者应理解并保持：

- **随机强密码**：`install.sh` 自动生成 16 位随机 `SERVER_PASSWORD`，不使用任何弱默认口令。
- **最小暴露**：仅 `80/443` 对公网开放；Koodo 数据源（8080）只经反代内部访问，不直连公网。
- **传输加密**：默认启用 TLS（自签或 Let's Encrypt）；纯 HTTP 模式仅建议在内网使用。
- **版本冻结**：生产环境建议 `koodo-hub freeze`，避免镜像浮动 tag 带来的不可控变更。
- **备份**：定期 `koodo-hub backup`，并以异地/异盘保留至少一份。

## 上游依赖

Koodo Reader 镜像由上游维护，漏洞修复取决于上游发布。冻结态会延迟安全更新，
请按 `status` 提示定期评估解冻升级。
